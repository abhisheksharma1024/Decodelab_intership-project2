# Decodelab_intership-project2
Responsive web layout built mobile-first using CSS Grid, Flexbox, and clamp() for fluid typography. Features sticky nav, hamburger menu, media queries at 600px &amp; 900px breakpoints, and accessible 44px touch targets. 
